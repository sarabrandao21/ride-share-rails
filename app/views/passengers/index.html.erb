<h2>Riders</h2>
<section class="button">
  <h3><%= link_to "Add a New Rider", new_passenger_path %></h3>
</section>
<h4></h4>
  
  <table>
    <tr>
      <th>Rider Name</th>
    </tr>
    <% @passengers.each do |passenger|  %>
      <tr>
        <td>
          <%= link_to passenger.name, passenger_path(passenger) %>
        </td>
      </tr>
  <% end %>
  </table>

